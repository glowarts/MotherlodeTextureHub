// Edit these two lines after you create the public repo, then commit.
// Everything else on the site is generated.
window.MOTHERLODE_CONFIG = {
  // GitHub owner/repo that receives texture submissions as issues.
  owner: "glowarts",
  repo: "MotherlodeTextureHub",

  // Shown in the submission issue so you know which build it came from.
  modVersion: "dev",
};
